<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//com.humanedu.pjt03//DTD Mapper 3.0//EN"
        "http://com.humanedu.pjt03/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.humanedu.pjt03.mapper.BoardMapper">

	<select id="getList" resultType="com.humanedu.pjt03.domain.BoardVO">
		<![CDATA[
			select * from tbl_board
		]]>
		order by bno <!--번호역순 -->
	</select>

	<insert id="insert">
		insert into tbl_board (bno, title, content, writer)
		values (seq_board.nextval, #{title}, #{content}, #{writer})
	</insert>

	<insert id="insertSelectKey">
		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
			select seq_board.nextval from dual
		</selectKey>
		insert into tbl_board (bno,title,content,writer)
		values (#{bno}, #{title}, #{content}, #{writer})
	</insert>

	<!--<select id="read" resultType="com.humanedu.pjt03.domain.BoardVO">
		select * from tbl_board
		where 1=1
		<if test="bno != null">
			and bno = #{bno}
		</if>
	</select>-->

	<select id="read" resultType="com.humanedu.pjt03.domain.BoardVO">
		select * from tbl_board where  bno = #{bno}
	</select>

	<delete id="delete">
		delete from tbl_board where bno = #{bno}
	</delete>

	<update id="update">
		update tbl_board
		set title = #{title},
		content = #{content},
		writer = #{writer},
		updateDate = sysdate
		where bno = #{bno}
	</update>

</mapper>